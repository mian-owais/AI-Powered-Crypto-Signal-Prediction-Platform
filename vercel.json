{
  "version": 2,
  "framework": "flask",
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": ".",
  "env": {
    "PYTHON_VERSION": "3.11"
  },
  "functions": {
    "api/*.py": {
      "memory": 3008,
      "maxDuration": 60,
      "runtime": "python3.11"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/app.py"
    },
    {
      "src": "/(.*)",
      "dest": "/api/app.py"
    }
  ]
}
